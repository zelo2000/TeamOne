import React, { FC } from 'react';
import { Button } from 'antd';
import './app.css';

const App: FC = () => (
  <div className="App">
    <Button type="primary">Button</Button>
  </div>
);

export default App;
